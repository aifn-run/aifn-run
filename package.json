{
  "name": "aifn-run",
  "private": true,
  "type": "module",
  "main": "./dist-server/index.mjs",
  "devDependencies": {
    "@types/node": "^22.13.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  },
  "scripts": {
    "ci": "npm ci && npm run build",
    "build": "rm -rf dist dist-server && mkdir -p dist dist-server && cp -R web/. dist/ && esbuild server/index.ts --bundle --platform=node --format=esm --outfile=dist-server/index.mjs"
  }
}
